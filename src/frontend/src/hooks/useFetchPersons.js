/* import { useQuery } from '@tanstack/react-query';
import { fetchPersons } from '../services/personService';

export default function useFetchPersons() {
  return useQuery(['persons'], fetchPersons);
}
 */
